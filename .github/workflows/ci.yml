name: CI — Tests

on:
    push:
        branches: [main]
    pull_request:
        branches: [main]

jobs:
    test:
        runs-on: ubuntu-latest

        services:
            postgres:
                image: postgres:18.3
                env:
                    POSTGRES_DB: test_pulsenotify
                    POSTGRES_USER: postgres
                    POSTGRES_PASSWORD: testpass123
                ports:
                    - 5432:5432
                options: >-
                    --health-cmd pg_isready
                    --health-interval 10s
                    --health-timeout 5s
                    --health-retries 5

            redis:
                image: redis:7-alpine
                ports:
                    - 6379:6379

        env:
            SECRET_KEY: test-secret-key-not-real
            DB_NAME: test_pulsenotify
            DB_USER: postgres
            DB_PASSWORD: testpass123
            DB_HOST: localhost
            DB_PORT: 5432
            DJANGO_SETTINGS_MODULE: pulsenotify.settings.local

        steps:
            - name: Checkout code
              uses: actions/checkout@v4

            - name: Set up Python
              uses: actions/setup-python@v5
              with:
                  python-version: "3.14"

            - name: Install dependencies
              run: pip install -r requirements.txt

            - name: Run migrations
              run: python manage.py migrate

            - name: Run tests
              run: python manage.py test
